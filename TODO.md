# TODO
- [x] Fix Price UI label range to show `₱0 - ₱850,000.00` (and same formatting across desktop/mobile).
- [ ] Ensure Price and Area slider UI use consistent formatting (PHP for price; consistent `Area (120-{areaMax})`).
- [ ] Update both HeroBanner component and the public page hero filter inputs if they render separately.
- [ ] Run typecheck/build if available (`npm run lint` / `npm run build`).


